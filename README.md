# Macro Recorder

Record, edit and replay mouse and keyboard macros on Windows.

```
uv sync
uv run macro-recorder              # GUI
uv run macro-recorder-cli --help   # CLI: record / play / list
uv run pytest
```
